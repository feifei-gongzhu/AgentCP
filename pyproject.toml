[project]
name = "agent-control-plane"
version = "3.3.0"
description = "Engineering control plane for blackboard-style security research agents"
requires-python = ">=3.10"
dependencies = ["keyring>=25", "playwright>=1.61"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

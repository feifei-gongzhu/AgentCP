[project]
name = "agent-control-plane"
version = "3.1.0"
description = "Engineering control plane for blackboard-style security research agents"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
